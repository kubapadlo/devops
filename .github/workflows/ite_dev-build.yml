name: ite_dev build

# Akcja reagująca na kontrybucje do dedykowanej gałęzi ite_dev
on:
  push:
    branches:
      - ite_dev
  pull_request:
    branches:
      - ite_dev
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ite_dev-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build na ite_dev
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Użycie własnej (dedykowanej) composite action z tego repo
      - name: Build aplikacji Next.js
        id: build
        uses: ./.github/actions/build-nextjs
        with:
          node-version: "22"
          pnpm-version: "9"

      # Załączenie zbudowanego artefaktu za pomocą dedykowanej akcji
      - name: Upload artefaktu (standalone)
        uses: actions/upload-artifact@v4
        with:
          name: kanye-countdown-build-${{ github.sha }}
          path: |
            .next/standalone
            .next/static
            public
          if-no-files-found: error
          retention-days: 7
